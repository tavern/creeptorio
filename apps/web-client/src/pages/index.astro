---
import db from '@pkgs/db'
import { App } from '../components/Canvas'
import BaseLayout from '../layouts/base.astro'

const map = await db.query.cells.findMany()
---

<BaseLayout>
  <App client:only="solid-js" map={map} />
</BaseLayout>
